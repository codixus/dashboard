# Required. This slug must exist in the backend dashboard config registry.
appslug=sample-app

# Optional for local development against a local backend. Cloudflare does not
# need this variable and uses the production control-plane URL by default.
# DASHBOARD_CONFIG_BASE_URL=http://localhost:3001/api/v1/dashboard-configs
